---
title: ツールチップ（CSSベース + aria）
description: data-tooltip属性で手早く導入。フォーカス/ホバー両対応。
tags: [tooltip, accessibility, ui]
frameworks: [Vanilla, Any]
level: basic
prompt_template: |
  CSSベースのツールチップ：
  - data-tooltip属性で内容指定
  - hover/focusで表示、モバイルでは長押し代替
  - aria-describedbyを自動で付与（JS補助）
---

import LiveDemo from '../../components/LiveDemo.astro';

<LiveDemo
  title="ツールチップのデモ"
  height="200px"
  html={`
<div style="display: flex; gap: 1rem; justify-content: center; align-items: center; height: 100px;">
  <button class="tip" data-tooltip="ダウンロード">⬇︎</button>
  <button class="tip" data-tooltip="お気に入りに追加">⭐</button>
  <button class="tip" data-tooltip="設定を開く">⚙️</button>
  <button class="tip" data-tooltip="ヘルプ">❓</button>
</div>
  `}
  css={`
.tip {
  position: relative;
  background: #1f2a37;
  border: 1px solid #374151;
  color: #e5edf4;
  padding: 0.5rem 1rem;
  border-radius: 8px;
  cursor: pointer;
  font-size: 1.5rem;
  transition: background 0.2s;
}
.tip:hover { background: #374151; }
.tip:focus-visible { outline: 2px solid #60a5fa; }
.tip::after {
  content: attr(data-tooltip);
  position: absolute; left: 50%; bottom: calc(100% + 8px);
  transform: translateX(-50%);
  background: #10161c; color: #e5edf4; border:1px solid #374151; padding: 6px 10px; border-radius: 6px;
  opacity: 0; pointer-events: none; transition: opacity .2s ease;
  white-space: nowrap; font-size: 12px;
}
.tip:hover::after, .tip:focus-visible::after { opacity: 1; }
  `}
/>

## 実装コード

**HTML**
```html
<button class="tip" data-tooltip="ダウンロード">⬇︎</button>
```

**CSS**
```css
.tip { position: relative; }
.tip:focus-visible { outline: 2px solid #60a5fa; }
.tip::after {
  content: attr(data-tooltip);
  position: absolute; left: 50%; bottom: calc(100% + 6px);
  transform: translateX(-50%);
  background: #10161c; color: #e5edf4; border:1px solid #1f2a37; padding: 4px 6px; border-radius: 6px;
  opacity: 0; pointer-events: none; transition: opacity .12s ease;
  white-space: nowrap; font-size: 12px;
}
.tip:hover::after, .tip:focus-visible::after { opacity: 1; }
```