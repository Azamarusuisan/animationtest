---
title: "Vanilla JavaScriptのベストプラクティス"
description: "モダンなJavaScriptの書き方。ES6+の機能を活用した読みやすく保守性の高いコード"
tags: [javascript, es6, best-practices, modern-js, clean-code]
category: javascript
level: intermediate
date: 2025-11-11
---

import LiveDemo from '../../components/LiveDemo.astro';

## モダンJavaScriptの書き方

ES6（ES2015）以降、JavaScriptは大きく進化しました。アロー関数、分割代入、テンプレートリテラルなど、コードを簡潔で読みやすくする機能が追加されています。

<LiveDemo
  title="モダンJavaScript vs 古い書き方"
  height="650px"
  html={`
<div style="max-width: 900px; margin: 0 auto;">
  <h3 style="margin-bottom: 1.5rem; color: #60a5fa;">コード変換デモ</h3>

  <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem; margin-bottom: 2rem;">
    <!-- 古い書き方 -->
    <div>
      <h4 style="color: #ef4444; margin-bottom: 1rem; font-size: 1rem;">❌ ES5（古い書き方）</h4>
      <pre style="background: #1f2a37; padding: 1rem; border-radius: 8px; font-size: 0.75rem; line-height: 1.6; overflow-x: auto; margin: 0; border: 2px solid #ef4444;"><code style="color: #e5edf4;">// 変数宣言
var name = 'Taro';
var age = 30;

// 関数
function greet(name) {
  return 'Hello, ' + name;
}

// オブジェクト操作
var user = {
  name: name,
  age: age
};

var userName = user.name;
var userAge = user.age;

// 配列操作
var numbers = [1, 2, 3];
var doubled = [];
for (var i = 0; i < numbers.length; i++) {
  doubled.push(numbers[i] * 2);
}</code></pre>
    </div>

    <!-- モダンな書き方 -->
    <div>
      <h4 style="color: #10b981; margin-bottom: 1rem; font-size: 1rem;">✅ ES6+（モダンな書き方）</h4>
      <pre style="background: #1f2a37; padding: 1rem; border-radius: 8px; font-size: 0.75rem; line-height: 1.6; overflow-x: auto; margin: 0; border: 2px solid #10b981;"><code style="color: #e5edf4;">// 変数宣言
const name = 'Taro';
const age = 30;

// アロー関数
const greet = (name) => \`Hello, \${name}\`;

// オブジェクト省略記法
const user = { name, age };

// 分割代入
const { name: userName, age: userAge } = user;

// 配列メソッド
const numbers = [1, 2, 3];
const doubled = numbers.map(n => n * 2);</code></pre>
    </div>
  </div>

  <!-- インタラクティブデモ -->
  <div style="padding: 1.5rem; background: #1f2a37; border-radius: 8px; border: 2px solid #374151;">
    <h4 style="color: #60a5fa; margin-bottom: 1rem;">実行デモ</h4>

    <div style="margin-bottom: 1rem;">
      <input
        type="text"
        id="user-name"
        placeholder="名前を入力"
        value="Taro Yamada"
        style="width: 100%; padding: 0.75rem; background: #0c1218; border: 2px solid #374151; border-radius: 6px; color: #e5edf4; font-size: 1rem;"
      >
    </div>

    <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 1rem; margin-bottom: 1rem;">
      <button id="demo-spread" class="action-btn">
        📋 Spread演算子
      </button>
      <button id="demo-destructure" class="action-btn">
        🔧 分割代入
      </button>
      <button id="demo-optional" class="action-btn">
        ❓ Optional Chaining
      </button>
      <button id="demo-nullish" class="action-btn">
        💫 Nullish Coalescing
      </button>
      <button id="demo-template" class="action-btn">
        📝 テンプレート
      </button>
      <button id="demo-array" class="action-btn">
        🔢 配列メソッド
      </button>
    </div>

    <div id="result" style="background: #0c1218; padding: 1rem; border-radius: 6px; font-family: monospace; font-size: 0.875rem; color: #10b981; min-height: 100px;"></div>
  </div>
</div>
  `}
  css={`
    .action-btn {
      padding: 0.75rem;
      background: #374151;
      border: 2px solid #4b5563;
      border-radius: 6px;
      color: #e5edf4;
      cursor: pointer;
      font-size: 0.875rem;
      transition: all 0.2s;
    }
    .action-btn:hover {
      background: #4b5563;
      border-color: #60a5fa;
      transform: translateY(-2px);
    }
  `}
  js={`
    const result = document.getElementById('result');
    const nameInput = document.getElementById('user-name');

    function showResult(title, code, output) {
      result.innerHTML = \`
        <div style="color: #60a5fa; margin-bottom: 0.5rem; font-weight: bold;">\${title}</div>
        <div style="color: #9ca3af; margin-bottom: 0.5rem;">// \${code}</div>
        <div style="color: #10b981;">→ \${output}</div>
      \`;
    }

    // Spread演算子
    document.getElementById('demo-spread').addEventListener('click', () => {
      const arr1 = [1, 2, 3];
      const arr2 = [4, 5, 6];
      const combined = [...arr1, ...arr2];
      showResult(
        'Spread演算子',
        'const combined = [...arr1, ...arr2]',
        JSON.stringify(combined)
      );
    });

    // 分割代入
    document.getElementById('demo-destructure').addEventListener('click', () => {
      const user = {
        name: nameInput.value,
        age: 30,
        email: 'user@example.com'
      };
      const { name, age } = user;
      showResult(
        '分割代入',
        'const { name, age } = user',
        \`name: "\${name}", age: \${age}\`
      );
    });

    // Optional Chaining
    document.getElementById('demo-optional').addEventListener('click', () => {
      const user = { address: undefined };
      const city = user.address?.city ?? 'Unknown';
      showResult(
        'Optional Chaining',
        'user.address?.city ?? "Unknown"',
        \`"\${city}"\`
      );
    });

    // Nullish Coalescing
    document.getElementById('demo-nullish').addEventListener('click', () => {
      const count = 0;
      const value = count ?? 10;
      showResult(
        'Nullish Coalescing',
        'const value = count ?? 10  // count = 0',
        \`value = \${value} (0は有効な値として扱われる)\`
      );
    });

    // テンプレートリテラル
    document.getElementById('demo-template').addEventListener('click', () => {
      const name = nameInput.value;
      const greeting = \`Hello, \${name}! Welcome to ES6+\`;
      showResult(
        'テンプレートリテラル',
        \`\\\`Hello, \\\${name}! Welcome to ES6+\\\`\`,
        \`"\${greeting}"\`
      );
    });

    // 配列メソッド
    document.getElementById('demo-array').addEventListener('click', () => {
      const numbers = [1, 2, 3, 4, 5];
      const result = numbers
        .filter(n => n % 2 === 0)
        .map(n => n * 2)
        .reduce((a, b) => a + b, 0);
      showResult(
        '配列メソッドチェーン',
        'numbers.filter().map().reduce()',
        \`[1,2,3,4,5] → 偶数のみ → 2倍 → 合計 = \${result}\`
      );
    });

    // 初期表示
    showResult('デモ', 'ボタンをクリックしてください', '結果がここに表示されます');
  `}
/>

## 変数宣言: const と let

### 基本ルール

```javascript
// ❌ 悪い例: var を使用（スコープ問題）
var count = 0;
if (true) {
  var count = 1; // 同じ変数を上書き
}
console.log(count); // 1

// ✅ 良い例: const と let を使用
let count = 0;
if (true) {
  let count = 1; // ブロックスコープ内の別の変数
}
console.log(count); // 0

// ✅ 最良: 再代入しない場合は const
const MAX_SIZE = 100;
const config = { theme: 'dark' };

// constでもオブジェクトの中身は変更可能
config.theme = 'light'; // OK
```

### 使い分けガイド

```javascript
// 1. デフォルトは const
const user = { name: 'Taro' };
const items = [1, 2, 3];

// 2. 再代入が必要な場合のみ let
let count = 0;
count++; // OK

// 3. var は使わない
// var は関数スコープで予期しない動作を引き起こす
```

## アロー関数

### 基本的な使い方

```javascript
// ❌ 古い書き方
const greet = function(name) {
  return 'Hello, ' + name;
};

// ✅ アロー関数（暗黙のreturn）
const greet = (name) => \`Hello, \${name}\`;

// 複数パラメータ
const add = (a, b) => a + b;

// パラメータが1つなら括弧を省略可能
const double = n => n * 2;

// 複数行の場合
const process = (data) => {
  const cleaned = clean(data);
  const validated = validate(cleaned);
  return validated;
};
```

### thisの扱い

```javascript
// ❌ 悪い例: thisが失われる
class Counter {
  constructor() {
    this.count = 0;

    setTimeout(function() {
      this.count++; // thisはundefined
    }, 1000);
  }
}

// ✅ 良い例: アロー関数でthisを保持
class Counter {
  constructor() {
    this.count = 0;

    setTimeout(() => {
      this.count++; // thisは正しく参照される
    }, 1000);
  }
}
```

## 分割代入

### オブジェクトの分割代入

```javascript
const user = {
  name: 'Taro',
  age: 30,
  email: 'taro@example.com',
  address: {
    city: 'Tokyo',
    country: 'Japan'
  }
};

// ❌ 古い書き方
const name = user.name;
const age = user.age;
const email = user.email;

// ✅ 分割代入
const { name, age, email } = user;

// 別名を付ける
const { name: userName, age: userAge } = user;

// デフォルト値
const { role = 'user' } = user;

// ネストしたオブジェクト
const { address: { city, country } } = user;

// 関数の引数で使用
function greet({ name, age }) {
  return \`\${name} is \${age} years old\`;
}
greet(user);
```

### 配列の分割代入

```javascript
const numbers = [1, 2, 3, 4, 5];

// ❌ 古い書き方
const first = numbers[0];
const second = numbers[1];

// ✅ 分割代入
const [first, second] = numbers;

// スキップ
const [first, , third] = numbers; // 1, 3

// Rest演算子
const [first, ...rest] = numbers; // first: 1, rest: [2,3,4,5]

// スワップ
let a = 1, b = 2;
[a, b] = [b, a]; // a: 2, b: 1
```

## Spread演算子とRest演算子

### Spread演算子

```javascript
// 配列のコピー
const original = [1, 2, 3];
const copy = [...original];

// 配列の結合
const arr1 = [1, 2];
const arr2 = [3, 4];
const combined = [...arr1, ...arr2]; // [1, 2, 3, 4]

// オブジェクトのマージ
const defaults = { theme: 'light', lang: 'ja' };
const userPrefs = { theme: 'dark' };
const config = { ...defaults, ...userPrefs }; // { theme: 'dark', lang: 'ja' }

// 関数の引数
const numbers = [1, 2, 3];
Math.max(...numbers); // 3
```

### Rest演算子

```javascript
// 関数の可変長引数
function sum(...numbers) {
  return numbers.reduce((a, b) => a + b, 0);
}
sum(1, 2, 3, 4); // 10

// オブジェクトの残りのプロパティ
const user = { name: 'Taro', age: 30, email: 'taro@example.com' };
const { name, ...rest } = user;
// name: 'Taro'
// rest: { age: 30, email: 'taro@example.com' }
```

## テンプレートリテラル

```javascript
const name = 'Taro';
const age = 30;

// ❌ 古い書き方
const message = 'Hello, ' + name + '! You are ' + age + ' years old.';

// ✅ テンプレートリテラル
const message = \`Hello, \${name}! You are \${age} years old.\`;

// 複数行
const html = \`
  <div class="user">
    <h2>\${name}</h2>
    <p>Age: \${age}</p>
  </div>
\`;

// 式の埋め込み
const price = 1000;
const tax = 0.1;
const total = \`Total: \${price * (1 + tax)} yen\`;

// タグ付きテンプレート
function highlight(strings, ...values) {
  return strings.reduce((result, str, i) => {
    return \`\${result}\${str}<mark>\${values[i] || ''}</mark>\`;
  }, '');
}

const name = 'Taro';
const html = highlight\`Hello, \${name}!\`; // Hello, <mark>Taro</mark>!
```

## 配列メソッド

### map, filter, reduce

```javascript
const users = [
  { name: 'Taro', age: 30, active: true },
  { name: 'Hanako', age: 25, active: false },
  { name: 'Jiro', age: 35, active: true }
];

// map: 変換
const names = users.map(user => user.name);
// ['Taro', 'Hanako', 'Jiro']

// filter: フィルタリング
const activeUsers = users.filter(user => user.active);
// [{ name: 'Taro', ... }, { name: 'Jiro', ... }]

// reduce: 集約
const totalAge = users.reduce((sum, user) => sum + user.age, 0);
// 90

// メソッドチェーン
const result = users
  .filter(user => user.active)
  .map(user => user.name)
  .join(', ');
// 'Taro, Jiro'
```

### find, some, every

```javascript
const users = [
  { id: 1, name: 'Taro' },
  { id: 2, name: 'Hanako' },
  { id: 3, name: 'Jiro' }
];

// find: 条件に合う最初の要素
const user = users.find(u => u.id === 2);
// { id: 2, name: 'Hanako' }

// some: 1つでも条件を満たすか
const hasAdmin = users.some(u => u.role === 'admin');
// false

// every: 全て条件を満たすか
const allActive = users.every(u => u.active === true);
// false
```

## Optional Chaining と Nullish Coalescing

### Optional Chaining (?.)

```javascript
const user = {
  name: 'Taro',
  address: {
    city: 'Tokyo'
  }
};

// ❌ 古い書き方
const zipCode = user && user.address && user.address.zipCode;

// ✅ Optional Chaining
const zipCode = user?.address?.zipCode;

// 配列でも使用可能
const firstItem = items?.[0];

// メソッド呼び出しでも使用可能
const result = obj.method?.();
```

### Nullish Coalescing (??)

```javascript
// ❌ || は falsy な値（0, '', false）も除外
const count = 0;
const value = count || 10; // 10（0が除外される）

// ✅ ?? は null/undefined のみ除外
const count = 0;
const value = count ?? 10; // 0（0は有効な値）

// 設定のデフォルト値
const config = {
  timeout: settings.timeout ?? 3000,
  retries: settings.retries ?? 3,
  enabled: settings.enabled ?? true
};
```

## クラス構文

```javascript
// ❌ 古い書き方: プロトタイプ
function Person(name, age) {
  this.name = name;
  this.age = age;
}

Person.prototype.greet = function() {
  return 'Hello, ' + this.name;
};

// ✅ クラス構文
class Person {
  constructor(name, age) {
    this.name = name;
    this.age = age;
  }

  greet() {
    return \`Hello, \${this.name}\`;
  }

  // ゲッター
  get info() {
    return \`\${this.name} (\${this.age})\`;
  }

  // セッター
  set age(value) {
    if (value < 0) throw new Error('Age must be positive');
    this._age = value;
  }

  // 静的メソッド
  static create(name, age) {
    return new Person(name, age);
  }
}

// 継承
class Student extends Person {
  constructor(name, age, grade) {
    super(name, age);
    this.grade = grade;
  }

  greet() {
    return \`\${super.greet()}, I'm a student\`;
  }
}
```

## モジュール

### エクスポート

```javascript
// 名前付きエクスポート
export const API_URL = 'https://api.example.com';
export function fetchUser(id) {
  // ...
}

// デフォルトエクスポート
export default class UserService {
  // ...
}

// まとめてエクスポート
const helper1 = () => {};
const helper2 = () => {};
export { helper1, helper2 };
```

### インポート

```javascript
// 名前付きインポート
import { fetchUser, API_URL } from './api.js';

// デフォルトインポート
import UserService from './UserService.js';

// エイリアス
import { fetchUser as getUser } from './api.js';

// 全てをインポート
import * as api from './api.js';

// 副作用のみ
import './polyfills.js';
```

## Promise と async/await

```javascript
// ❌ コールバック地獄
fetchUser(id, (user) => {
  fetchPosts(user.id, (posts) => {
    fetchComments(posts[0].id, (comments) => {
      // ...
    });
  });
});

// ✅ Promise チェーン
fetchUser(id)
  .then(user => fetchPosts(user.id))
  .then(posts => fetchComments(posts[0].id))
  .then(comments => {
    // ...
  })
  .catch(error => console.error(error));

// ✅✅ async/await
async function loadUserData(id) {
  try {
    const user = await fetchUser(id);
    const posts = await fetchPosts(user.id);
    const comments = await fetchComments(posts[0].id);
    return { user, posts, comments };
  } catch (error) {
    console.error('Failed to load data:', error);
  }
}
```

## まとめ

### 必ず使うべき機能

1. **const/let**: varは使わない
2. **アロー関数**: 簡潔な関数定義
3. **テンプレートリテラル**: 文字列結合
4. **分割代入**: オブジェクト/配列の展開
5. **Spread演算子**: 配列/オブジェクトのコピー

### 積極的に使うべき機能

6. **配列メソッド**: map, filter, reduce
7. **Optional Chaining**: 安全なプロパティアクセス
8. **Nullish Coalescing**: デフォルト値の設定
9. **async/await**: 非同期処理
10. **クラス構文**: オブジェクト指向

### コードの品質向上

- 可読性: 短く明確なコード
- 保守性: バグの少ないコード
- パフォーマンス: 効率的な処理

## Claude Codeへの指示

このTipsをClaude Codeに実装してもらう場合、以下の点を指示してください：

- 1. 明確で理解しやすいコードを書く
- 2. コメントで重要な箇所を説明
- 3. エッジケースを考慮
- 4. ベストプラクティスに従う
- 5. 実装例とドキュメントを提供

### プロンプト例

```
Vanilla JavaScriptのベストプラクティスを実装してください。

要件：
モダンなJavaScriptの書き方。ES6+の機能を活用した読みやすく保守性の高いコード

以下の点を守ってください：
1. 明確で理解しやすいコードを書く
2. コメントで重要な箇所を説明
3. エッジケースを考慮
4. ベストプラクティスに従う
5. 実装例とドキュメントを提供
```

